server:
  port: 9296

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/manishthakur/spring-cloud-config-server.git
          clone-on-start: true
